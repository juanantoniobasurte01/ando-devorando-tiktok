# Ando Devorando - TikTok integration site

Technical site used by the Ando Devorando Content Posting API integration.

It contains the OAuth callback, privacy policy, terms, application assets and TikTok domain-verification file. Temporary carousel media is added under `media/` only after explicit user approval and removed after TikTok reports `PUBLISH_COMPLETE`.
